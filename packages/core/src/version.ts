/**
 * The release this build is. Every package.json in the repository carries the
 * same number, and version.test.ts holds them to it: a release bumps all of
 * them and this together. `clickmonk version` prints it.
 */
export const VERSION = '0.1.0'
