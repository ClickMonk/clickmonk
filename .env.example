# Copy to .env and replace every value. Generate each secret with:
#   openssl rand -hex 32
POSTGRES_PASSWORD=change-me
CLICKHOUSE_PASSWORD=change-me
CLICKMONK_SECRET=change-me-to-at-least-32-characters-long
